<p id="notice"><%= notice %></p>
<div class="bs-docs-section">

  <div class="row">
    <div class="col-lg-16">
      <div class="page-header">
      <h1 id="tables">Liste des utilisateurs</h1>
      </div>
      <%= link_to 'Nouvel utilisateur', new_user_path %>
      <div class="bs-component">
        <table class="table table-striped table-hover ">
          <thead>
            <tr>
              <th>Username</th>
              <th>Password</th>
              <th>Email</th>
              <th>Role</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td><%= @user.username %></td>
              <td><%= @user.password %></td>
              <td><%= @user.email %></td>
              <td><%= @user.role %></td>
              <td><%= link_to 'Editer', edit_user_path(@user) %></td>
              <td><%= link_to 'Retour', users_path %></td>
            </tr>
          </tbody>
        </table> 
      </div><!-- /example -->
    </div>
  </div>
</div>

